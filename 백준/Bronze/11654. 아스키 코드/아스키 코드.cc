#include <bits/stdc++.h>

using namespace std;

char X;

int main()
{
    ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
    
    cin >> X;
    cout << (int)X;

    return 0;
}
#include <bits/stdc++.h>

using namespace std;

int S;

int main()
{
    ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
    
    string s;
    while (getline(cin, s)) S++;
    cout << S;

    return 0;
}
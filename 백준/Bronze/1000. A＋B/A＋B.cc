#include <bits/stdc++.h>

using namespace std;

int A, B, S;

int main()
{
    ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
    
    // input
    cin >> A >> B;

    // solution
    S = A + B;

    // output
    cout << S;

    return 0;
}
#include <bits/stdc++.h>

using namespace std;

long long A, B, C, S;

int main()
{
    ios::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
    
    cin >> A >> B >> C;
    S = A + B + C;
    cout << S;

    return 0;
}